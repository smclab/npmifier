#!/usr/bin/env node

var program = require('commander');
var pkginfo = require('pkginfo')(module);

program
	.version(exports.version, '-v, --version')
	.description(exports.description)
	.usage('COMMAND [ARGS] [OPTIONS]')
	.command('convert', "npmifies an existing classic Titanium SDK application")
	.command('analyse', "ensures that require’s paths are correct")

program.parse(process.argv);

if (!program.args.length) program.help();

/*var command = program.args[0];

if (!command) {
	porgram.help();
}
else {
	require('./npmify-' + command)(program);
}*/

/*require('../src/tiapp').fromPath(process.cwd()).done(function (tiapp) {
	console.dir(tiapp.getPlugins());

	console.dir(tiapp.installPlugin('npmify'));

	console.log(" -- IPHONE ---")
	console.dir(tiapp.getModulesFor({ platform: 'iphone' }));

	console.log(" -- IPHONE and COMMONJS ---")
	console.dir(tiapp.getModules({ platforms: [ 'iphone', 'commonjs' ] }));

	//console.log(tiapp.toString());
});*/
